---
description: Delete role(s)
enabled: true
entry_point: src/wrapper.py
name: role.delete
parameters:
    base:
        default: role delete
        immutable: true
        type: string
    domain:
        description: Domain the role belongs to
        type: string
        default: default
    ep:
        default: EntryPoint.parse('role_delete = openstackclient.identity.v3.role:DeleteRole')
        immutable: true
        type: string
    role:
        description: Role to delete (name or ID)
        required: true
        type: string
runner_type: python-script
